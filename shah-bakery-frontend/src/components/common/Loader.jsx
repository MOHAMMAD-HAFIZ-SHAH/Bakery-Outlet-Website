const Loader = () => {
  return (
    <div className="fixed inset-0 bg-[var(--cream)] flex items-center justify-center z-[999]">

      <div className="w-20 h-20 border-[6px] border-[var(--gold)] border-t-transparent rounded-full animate-spin" />

    </div>
  );
};

export default Loader;